# Handoff notes — async decision queue for Alena

**Status: empty.** All six items from the May 19/23 queue were answered
by Alena on 2026-06-11 and shipped the same day (sessions 63–64):

1. Trust copy softened sitewide (no more HIPAA / physician-review claims).
2. Newsletter form wired to a real `newsletter_subscribers` table.
3. One-step appointment reschedule (endpoint + dialog on both surfaces).
4. HEIC/HEIF uploads decode server-side now.
5. Booking auto-creates a *pending* doctor connection (patient still approves).
6. Doctor chart-access audit log + "Chart Access History" in Settings.

Nothing currently awaits a decision.
